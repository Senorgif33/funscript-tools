## What's New in v2.5.1

### Bug Fixes

1. **Custom Event Builder timeline drag** — Window chrome sash re-layout no longer runs on child Configure events (e.g. timeline redraws). Sashes update only when the dialog size itself changes, so dragging events on the timeline stays responsive.
